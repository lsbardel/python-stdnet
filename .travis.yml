language: python

python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "pypy"

services:
  - redis-server

install:
  - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip install --use-mirrors argparse unittest2; fi
  - pip install pulsar
  - python setup.py install
script: python -m runtests regression --verbosity 2

branches:
  only:
    - master
